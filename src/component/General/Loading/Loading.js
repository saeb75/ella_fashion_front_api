import React from "react";
import loading from "../../../assents/loading.svg";
const Loading = () => {
  return (
    <div className="container" className="loading_container">
      <img src={loading} />
    </div>
  );
};

export default Loading;
